# U7 — E2E 실작동 검사 + 즉시패치 — 상황 보고

**Date**: 2026-05-30  
**Status**: ⏸️ **blocked** (harness2-wf watchdog stuck, Python env missing)  
**Priority**: 1 (Phase I 진입 선행)  

---

## 현 상황

### 팀메이트 (harness2-wf watchdog)
```
watchdog_result: stuck
- silent_log_s: 366초 (마지막 로그)
- silent_commit_s: 6500초 (마지막 커밋)
- status: in-process 상태 불명, 응답 없음
```

**원인 추측**:
- Worker/Verifier 프로세스 hang 또는 crash
- SendMessage 불가 (watchdog role = relay 금지)
- harness2 transport 불안정 (progress.md 언급: "harness2 부적합—직렬 spine·실주문 경로·transport 불안정")

**영향**: 
- harness2-wf 하에서 U7 진행 불가
- Worker가 main 직접 진입 필요

---

### 환경 상태
```
✅ Git repo: clean (HEAD 6602338)
❌ Python: PATH 미설정 (pytest/python3 command not found)
❌ venv: 확인 불가 (경로 접근 실패)
```

**영향**:
- Unit test (pytest) 직접 실행 불가
- Python scripts 실행 불가
- 환경 초기화 후 U7 재시작 필요

---

## U7 게획 (4단계)

### §1 데이터 수집 (low risk)
- `collect_market_data.py` — Upbit API + RSI/SMA/MACD/BB
- `collect_fear_greed.py` — Alternative.me FGI
- `collect_news.py` / `collect_rss_news.py` — 뉴스
- `capture_chart.py` — Playwright

**검증**: JSON 산출, 필드 정합성, timestamp 일치

### §2 결정 엔진 (medium risk)
- 보수적/보통/공격적 에이전트 각 실행
- 결정 schema (type_CHECKING) 정합성
- confidence/breakdown 동치 검증

**검증**: Decision output schema, confidence ∈ [0, 100], breakdown 필드

### §3 위험 제어 + Orchestrator (high risk)
- Orchestrator.run() — 전략 전환 (danger_score, opportunity_score)
- KillSwitch FSM H27 상태머신
- Auto-emergency 발동 조건
  - 연속 손절 5회+ (30점)
  - 24h -10% 급락 (25점)
  - cascade + danger 동시
  - 외부 약세 5개+ 겹침
- De-risk executor — 전량 매도 경로

**검증**: 점수계산, 전환 쿨다운, auto-emergency.json 상태 기록

### §4 통합 + 회귀 (critical)
- pytest Phase -1~R 회귀 0 검증
- Reconciliation liveness
- SACRED validation (execute_trade diff = 0)
- DRY_RUN=true / EMERGENCY_STOP 기본값 유지

**검증**: 테스트 PASS, no regression, commit history clean

---

## 차단 요소

| 요소 | 상태 | 해결 | 우선순위 |
|------|------|------|---------|
| harness2 watchdog | stuck | re-spawn 또는 main 직접 | P0 |
| Python env | missing | `cd /d D:\projects\Inv && python -m venv .venv` + activate | P0 |
| watchdog timeout | 진행 중 | ~540초 대기 (~9분) | P1 |

---

## 권장 다음 스텝

1. **watchdog 타임아웃 대기** (540초, ~9분)
   - timeout 후 결과 확인
   - stuck 확정 시 harness2 재시작 또는 abandon

2. **Python 환경 초기화** (5분)
   ```bash
   cd D:\projects\Inv
   python -m venv .venv
   .venv\Scripts\activate
   pip install -r requirements.txt
   ```

3. **Unit test 회귀 검증** (10분)
   ```bash
   pytest tests/ -q --tb=short -k "Phase -1 or Phase 0 or Phase R"
   ```

4. **Data collection 샘플 검사** (15분)
   - 각 스크립트 단독 실행
   - JSON 산출 확인
   - .env 여부 확인 (FGI/News API key)

5. **Integration test** (30분)
   - DRY_RUN=true 상태 `run_agents.sh` 또는 Python 에이전트 실행
   - Decision 산출, KillSwitch 상태 기록

6. **최종 SACRED 검증** (5분)
   - `git diff` execute_trade.py / base_agent.py (SACRED 보존)
   - 결함 발견 시 즉시패치 + 회귀 검증

**총 소요**: ~90분 (Python env 제외)

---

## 산출물 (예정)

- [ ] **U7-findings-20260530.md** — 발견사항 + 근본원인 분석
- [ ] **즉시패치 commits** — 각 발견 per patch
- [ ] **Phase I 진입 보고서** — go/no-go + 리스크 평가

---

## Notes

- **GOLDEN RULE**: IMPLEMENTATION_PROMPT.md 불가침 (일탈 금지)
- **SACRED**: execute_trade 실주문/nonce/run_cycle diff = 0 유지 의무
- **DRY_RUN**: 모든 테스트는 DRY_RUN=true (실주문 0)
- **회귀**: Phase -1~R 전수 (339a710..HEAD)

---

**Status**: Waiting for (1) watchdog timeout / (2) Python env setup  
**Assigned**: Agent (haiku)  
**Blocked by**: harness2-wf transport  
