# FHC 단일 계약 (btn-Inv ↔ btn-button 공유 primitive) — 2벌 회피 SSOT

> btn-button "정렬 동의 + 인프라 2벌 회피 합의"(2026-06-03) 후속. 양 트랙이 **이 한 schema**로만 빌드.
> 설계 근거 = `.consult-judge-report-RESULTS.md` C1~C16. 본 파일 = 빌드 계약(인터페이스 고정).

## 1. 소유 분담 제안 (2벌 차단)
| 모듈 | 위치 | 소유 | 내용 |
|---|---|---|---|
| **FHC core**(자산무관) | `core/assume/` | **btn-Inv** | FHC schema·2-leg·5-state lifecycle·bonus 채널(L1+bonus≤C)·INV-1..16 집행 |
| **Equity 인스턴스** | `stock/` | **btn-button** | exposure card(=FHC 어댑터)·Σ_signal mixing(=메타카드)·construction→IOC |
| **Macro 인스턴스** | `core/`/study | btn-Inv | macro FHC 카드(리서치 발권)·mediator(state-space 구독) |
- ★core = 자산무관 단일 구현(btn-Inv). equity/macro는 그 위 어댑터. = 2벌 0.
- button의 construction seam은 **core bonus 채널 API를 소비**(직접 사이징 안 함). WireSmith가 배선.

## 2. FHC schema (자산무관 계약, 고정)
```
FHC {
  card_id: uuid; schema_version: int
  source: {doc_ref, extract_run_id}; author_model: enum{claude,gemini,human}
  minted_at_event: ts; minted_at_know: ts            # PIT dual-stamp
  thesis_text: str
  scope: {type: enum{macro,sector,single_asset}, targets:[asset_id], universe_ref}
  direction: enum{long_tilt, short_tilt(inverse×1), de_risk}
  bonus_cap: float                                    # = per-asset headroom (C − L1), 구조값(confidence 무관)
  mediator: {observable_ref, test: enum{deterministic_threshold, conf_sequence, state_space_posterior},
             condition:{op,threshold,window}, compute_delay, state: enum{holds,fails,unknown},
             eval_at_event, eval_at_know}
  outcome: {metric: enum{ts_rank_IC(macro), cs_rank_IC(sector)}, target_signal_ref,
            e_process:{e_value,log_wealth,lambda_t}, cusum:{S,threshold,hysteresis_band},
            conditional_on_mediator: bool=true, eval_at_event, eval_at_know}
  state: enum{minted,confirmed,vacated,rejected,revived}
  state_history:[{from,to,reason,at_event,at_know}]   # append-only event-sourced
  confirm_threshold; reject_threshold; alpha_wealth_ref
  realized_bonus: float                               # = monotone(log_wealth) clipped at bonus_cap
  revival_count: int
}
```
- ★equity exposure card = scope.type=sector + outcome.metric=cs_rank_IC + mediator=섹터 펀더멘털(compute_delay·restatement PIT 주의)인 **FHC 한 인스턴스**. 별 schema 아님.

## 3. lifecycle 5-state (전이·invariant 고정)
| 전이 | 조건 |
|---|---|
| minted→confirmed | mediator.holds AND outcome.e_value≥confirm_threshold(alpha-wealth 차감). bonus 적립 개시 |
| minted→rejected | mediator.holds인데 outcome e-CUSUM reject = 전제참·예측틀림 |
| confirmed→vacated | mediator holds→fails. 반증 아님(전제만 미충족). bonus suspend(→0), 카드 생존 |
| vacated→revived | mediator fails→holds 재성립 AND outcome 미기각. revival_count++. hysteresis k틱 |
| confirmed/revived→rejected | mediator holds인데 outcome CUSUM reject → clawback+사망 |
- ⛔ minted→vacated 금지(minted-dormant=적립0). ⛔ rejected=absorbing(부활=새 card_id). ⛔ revival_count 상한→retire.

## 4. bonus 채널 API (core 소유, equity construction이 소비)
- `emit_bonus(card) → float`: confirmed/revived 카드만. realized_bonus = monotone(log e-value), clip at bonus_cap=(C−L1). concave+thresholded(confirm_threshold 전 0).
- 사이징: `w_final = clip(L1 + Σ bonus, ceiling=C)`. **C=risk_gate 독립산출 단일자산 천장**(button construction의 per-name 1-3% cap = 이 C).
- gate=AND(mediator∧outcome 통과), size=log-additive(e-value 곱=log-wealth 합).

## 5. INV 계약 (양 트랙 공통 집행, core가 enforce)
- INV-1 w≤C(C는 bonus 독립·PIT-safe) / INV-3 fail-closed(오류→bonus0→L1이하) / INV-11 OFF=byte-identical / INV-12 alpha-wealth meta/basket 고정분할 firewall. (전 16개=RESULTS C13)
- INV-9 breaker = native RegimeGlasso transition 1차(button 소유 RegimeGlasso 구독), 외생 vol redundant. 회수=target0+IOC ladder(button construction).

## 6. 미해결 (button 회신 필요)
- (b) core 소유=btn-Inv 동의? button construction이 bonus API 소비 방식(WireSmith 배선) 동의?
- (c) button construction/exposure-card ETA → core bonus 채널 plug-in 시점.
- Σ_signal 원장 ↔ core alpha-wealth firewall(INV-12) 경계: meta(Σ_signal)=button / basket=button 내부 LORD++, core는 firewall만 강제?

## 6'. button 회신 (btn-button, 2026-06-06)
- (b) **Y** — core=FHC bonus 소유(btn-Inv enforce) + button construction.build_sleeve_decisions가 bonus API 소비 배선(WireSmith) 동의. ETF fallback도 동일 construction seam → bonus 곱이 selection/EW/ETF weight 위 적층 정합.
- (c) **ETA=go-live arming 시(날짜 미정)** — ETF fallback 골격 커밋 64e8bc0 dormant(호출처0). core bonus plug-in=사람 게이트 선결, 그 전 dormant. go-live=사용자 게이트라 button이 날짜 미확정.
- (Σ_signal/INV-12) **Y** — meta(Σ_signal)=button / basket LORD++=button 내부 / core=INV-12 firewall만 강제 동의. etf_beta_lock counterfactual·shadow-EW e-CUSUM=button logging(firewall 안쪽).

## 6''. btn-Inv 확인 + 수렴 (2026-06-06)
- (b1/b2)(c)(Σ) **합의 수신** — core/assume FHC core(fhc·bonus_channel·fhc_fdr·macro_mediator·spanning_gate·info_delta_gate) = btn-Inv 단일 소유, construction.build_sleeve_decisions가 size_with_bonus 소비(go-live arming 시), Σ_signal·INV-12 분담 확정.
- (RG) **§5 INV-9 기정의로 닫힘** — button 소유 RegimeGlasso가 sleeve cov transition native 1차 감지 → core `fhc.transition(breaker_tripped=True)` boolean 구독(S3a 구현 완료). macro mediator(`macro_mediator.py`=MacroView 거시국면 posterior 구독)와 **별 레이어**(전자=sleeve cov breaker / 후자=거시 posterior). 외생 vol redundant. 회수=IOC ladder(button construction). 추가 합의 불요.
- ★**수렴 확정**(btn-Inv·btn-button 양측): §6 4건 + RG 전부 합의/기정의 → **FHC 통합 계약 닫힘**. go-live arming(사람 게이트) 시 core bonus plug-in 실소비뿐. 양쪽 dormant·호출처0·INV-11 byte-identical 보존.
