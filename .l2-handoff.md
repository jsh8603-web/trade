---
tags: [type/handoff, domain/inv, topic/local-db-migration, session/btn-Inv]
date: 2026-05-31
note: L2 (Supabase REST→SQLite 어댑터 전환) 핸드오프. 매매 핵심 경로 6파일 완료+smoke 검증. 나머지 ~44 REST + psycopg2 파일은 L3~L5.
---

# L2 핸드오프 — Supabase REST → core.db SQLite 어댑터 전환

> 상위: plan-local-db-migration.md / progress-local-db-migration.md / handoff-local-db-migration-20260530.md

## ✅ 완료 — 매매 핵심 경로 6파일 (검증: REST=0 + AST OK + smoke OK)
- `scripts/save_decision.py` — 5함수(supabase_post→db.insert, update_past_performance/mark_feedback_applied→select+update, _update_embedding_via_sql→db.update 벡터 raw list, main 가드 제거). import OK.
- `scripts/execute_trade.py` — `_record_trade_to_db`→db.insert. ⛔ 실주문 orders POST(515)·JWT nonce SACRED **보존**. REST=0/AST OK/import OK.
- `scripts/dynamic_risk.py` — `_fetch_recent_decisions`→db.select + `get_adjustment` risk_adjustments→db.select. import OK.
- `scripts/run_agents.py` — 7곳(supabase_headers 헬퍼 제거, save_execution_log/save_market_data_record/Phase5 decisions·RAG fallback·portfolio_snapshots·feedback). REST=0/AST OK/import OK.
- `scripts/run_agents.sh` — heredoc 4블록(7a decisions=스킵유지, 7b market_data, 7c execution_logs, 7d portfolio_snapshots → db.insert). bash -n OK. 바깥 SUPABASE 가드 제거.
- `scripts/evaluate_switches.py` — agent_switches GET→db.select, PATCH→db.update. AST OK.

**SMOKE 검증 (2026-05-31)**: `INV_DB_BACKEND=sqlite` 로 save_decision(sample) → decisions id=1 INSERT(decision='관망' 매핑·confidence=0.72 정규화·source='llm'), execution_logs INSERT+SELECT, decisions SELECT 모두 정상. data/inv.db 생성됨. → **매매 파이프라인 로컬 SQLite 실동작 확인**.

## ⬜ 남은 작업 — ~44 REST 파일 + psycopg2 (L3~L5, 매매 비핵심)
`grep -rl 'rest/v1' scripts agents core rl_hybrid scalp_ml kimchirang utils | grep -v test` 로 최신 목록 확인. 대분류:
- **읽기/학습(L3)**: strategy_health, model_retrainer, regime_learner, regime_detector, model_diversity, auto_learner, phase_cache, recall, recall_rag(RAG 벡터), memory.
- **피드백/리포트**: feedback, feedback_hub, nl_feedback, performance_report, full_analysis_report, retrospective, scalp_retrospective, db_cleanup.
- **RL/RAG(L4)**: rl_hybrid/* (psycopg2 6파일 — config.py:54 SupabaseConfig SSOT 우선, data_collector·embedding_store 등), scalp_ml/*, train_*.py.
- **일회성 데이터(L5)**: collect_2018~2024, backfill_decisions, insert_historical_decisions, embed_historical_data, migrate_embeddings_to_gemini, reembed_to_bge, replace_macro_with_real.
- **통합/기타**: agents/{base_agent,external_data,orchestrator}, core/coin_track, kimchirang/{config,db}, utils/newsrang_reader, scripts/{supabase_query,telegram_chat,telegram_listener,web_server,worker_admin,version_manager,short_term_trader,training_scheduler}, lifeline/{diagnostician,healer,health_db_sync,sentinel}.

## ⚠️ 구조적 장애 (이 세션 관측)
1. **Agent 서브에이전트 채널 사망**: Agent 결과 `[Tool result missing due to internal error]` 8회 반복, 파일 미변경 → **이 세션은 메인 직접 Edit만 가능**. 새 세션에서 회복되면 G1~G6 그룹 병렬 위임으로 ~44파일 가속 가능(그룹 분할 = 대화 참조).
2. **도구출력 간헐 손상**: grep/Read 가 truncate·라인번호 흔들림·코드펜스 오염. **손상 Read 가 가짜 구조를 보여줘** 그걸로 Edit 시 "String not found". ★정확 Read(일관 출력) 확보 후 Edit + 매 Edit 후 grep 재검증(일관 형식 나올 때까지 재시도).

## 전환 규약 (6파일로 확립)
```
from core.db import db   # 파일 상단, sys.path 에 PROJECT_DIR(루트) 보장 후
db.insert(table, row)                  # ← requests.post(f"{url}/rest/v1/{table}", json=row)
db.select(table, filters={"col":"op.val"}, order="c.desc", limit=N, select="a,b")
                                       # ← requests.get(...params={col:"op.val","select","order","limit"})
db.update(table, filters, patch)       # ← requests.patch(...?id=eq.X, json=patch)
db.delete(table, filters)              # ← requests.delete
db.rpc_match(table, embedding, column="state_embedding", k=N, filters=None)  # pgvector match
db.execute_raw(sql, params)            # psycopg2 복잡 SQL (SELECT→list[dict], 그외→rowcount)
```
- filters = PostgREST 문법 그대로: eq./ne./gt./gte./lt./lte./in.(a,b)/is.null/like.*x*/ilike.
- 벡터 컬럼(state_embedding/embedding/decision_embedding)은 **raw list 그대로** → 어댑터 float32 BLOB 직렬화.
- **제거**: SUPABASE_URL/KEY 가드(`if not url: return`), requests Session/headers 헬퍼, Management API SQL fallback.
- **유지(미변경)**: skip_trade_db() 가드, machine_name(어댑터 자동제거), Upbit/외부 API(api.upbit.com), 임베딩 생성(Gemini/genai/BGE) 로직.
- heredoc(.sh): 블록 안 `import sys; from pathlib import Path; sys.path.insert(0,str(Path.cwd())); from core.db import db`, 바깥 bash SUPABASE 가드 제거.
- psycopg2: 단순 SELECT/INSERT=db.select/insert, 복잡(JOIN/집계/window)=db.execute_raw. pandas는 `pd.DataFrame(db.execute_raw(sql, params))`.

## SACRED (불변)
- ⛔ execute_trade.py 실주문(orders POST·:515·JWT nonce), rl_hybrid/rl/live_trader.py run_cycle 본체 미변경. DB I/O만 어댑터화.
- DRY_RUN/EMERGENCY_STOP/MAX_TRADE_AMOUNT 안전장치 미변경.

## 환경
- python = `C:/Users/jsh86/AppData/Local/Programs/Python/Python312/python.exe` (bash 의 `python`/`python3` = command not found). cwd = /d/projects/Inv. bash 셸(cmd 아님 — `cd /d` 금지).
- 검증: `grep -c 'rest/v1' {file}`(0목표) + `"$PY" -c "import ast; ast.parse(open('{file}',encoding='utf-8').read()); print('OK')"`.
- INV_DB_BACKEND=sqlite(기본), data/inv.db.

## 다음 순서
L3(읽기/학습) → L4(RL/RAG psycopg2) → L5(일회성) → 통합/기타. 도구·Agent 회복 시 서브 병렬. 마지막 L6: run_agents.sh DRY_RUN 전체 e2e + pytest 회귀.
