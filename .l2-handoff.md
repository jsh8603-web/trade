---
tags: [type/handoff, domain/inv, topic/local-db-migration, session/btn-Inv]
date: 2026-05-31
note: Supabase REST→SQLite 어댑터 전환. L2(매매 핵심 6파일) 완료·커밋. 다음=L3~L5(남은 ~50 REST + psycopg2 5). 새 세션 동등 재개용.
---

# 핸드오프 — Supabase → 로컬 SQLite 전환 (L2 완료, L3~L5 남음)

## 🟢 새 세션 진입 절차 (이 순서대로)
1. 이 파일 전체 Read (규약·SACRED·환경 다 여기 있음).
2. `progress-local-db-migration.md` Read — Steps 체크박스 + Working Notes 맨 위 ckpt.
3. `git log --oneline -3` — HEAD=b459d87(L2 커밋) 확인. 다르면 누가 더 커밋한 것.
4. 라우팅: plan 있음 → §B. 남은 작업=대량 정형 전환 → 도구/Agent 회복 시 **G1~G6 서브 병렬**, 아니면 메인 직접.

## ✅ 완료 — L0/L1(어댑터) + L2(매매 핵심 6파일)
- **L0/L1** (commit 이전): `core/db/` 어댑터 6파일 + `schema.sql`(48테이블) + `build_sqlite_schema.py`. tests/test_db_adapter_smoke.py.
- **L2** (commit **b459d87**): 매매 핵심 경로 6파일 전환 완료. 전부 REST=0 + AST OK(2회 일관) + module import OK + round-trip PASS.
  - `scripts/save_decision.py` — supabase_post→db.insert, update_past_performance/mark_feedback_applied→select+update, _update_embedding_via_sql→db.update(벡터 raw list).
  - `scripts/execute_trade.py` — `_record_trade_to_db`→db.insert. ⛔실주문 orders POST(:515)·JWT nonce 미변경.
  - `scripts/dynamic_risk.py` — `_fetch_recent_decisions`·risk_adjustments→db.select.
  - `scripts/run_agents.py` — supabase_headers 제거 + 7곳(execution_logs/market_data/Phase5 decisions·RAG fallback·portfolio_snapshots·feedback).
  - `scripts/run_agents.sh` — `$PYTHON -c` 4블록(Phase5 decisions/5b market_data·execution_logs/5c portfolio) + past_decisions 조회. 바깥 SUPABASE 가드 제거. bash -n OK.
  - `scripts/evaluate_switches.py` — agent_switches select/update.
  - `core/db/sqlite_backend.py` — schema 재적용 idempotent 버그 수정(`already another table or index` catch 추가). ← inv.db 재연결 시 깨지던 진짜 버그.
  - **round-trip 실증**: `INV_DB_BACKEND=sqlite python -c "save_decision(sample)"` → decisions id 발급+저장, execution_logs 저장/조회, data/inv.db 생성.

## ⬜ 남은 작업 — L3~L5 (~50 REST 파일 + psycopg2 5)
최신 목록: `grep -rl 'rest/v1' scripts agents core rl_hybrid scalp_ml kimchirang utils | grep -v test | grep -v __pycache__`
psycopg2: `grep -rl 'psycopg2' ...` → recall_rag.py, rl_hybrid/rag/embedding_store.py, rl_hybrid/rl/{data_collector,data_loader,rl_db_logger}.py

**그룹 분할 (서브 병렬용 G1~G6)**:
- **G1 읽기/학습(L3)**: strategy_health, model_retrainer, regime_learner, regime_detector, model_diversity, auto_learner, phase_cache, recall, memory.
- **G2 피드백/RAG/리포트**: feedback, feedback_hub, nl_feedback, recall_rag(벡터+psycopg2), performance_report, full_analysis_report, retrospective, db_cleanup, scalp_retrospective.
- **G3 rl_hybrid(L4)**: rl_hybrid/rl/{admin_review,hierarchical_rl,llm_state_encoder,offline_rl,rl_db_logger,self_tuning_rl,trainer_submit,weekly_retrain,live_trader,data_collector,data_loader}, rag/embedding_store. ⛔live_trader run_cycle 본체 미변경. config.py:54 SupabaseConfig(psycopg2 SSOT) 먼저.
- **G4 scalp_ml/train**: scalp_ml/{distributed_training,feature_engineer,train_lgbm,worker,week2/cql_bridge}, scripts/train_{1h_intensive,comprehensive,parallel,parallel_v2,parallel_v3}, training_scheduler.
- **G5 일회성 데이터(L5)**: collect_{2018_2021,2022,2024}_data, backfill_decisions_2018_2021, insert_historical_decisions, embed_historical_data, migrate_embeddings_to_gemini, reembed_to_bge, replace_macro_with_real.
- **G6 통합/기타**: agents/{base_agent,external_data,orchestrator}, core/coin_track, kimchirang/db, utils/newsrang_reader, scripts/{telegram_chat,telegram_listener,web_server,worker_admin,version_manager,short_term_trader,supabase_query}, lifeline/{healer,health_db_sync,sentinel}. + run_analysis.sh/.ps1.
- **참고**: core/db/supabase_backend.py 의 rest/v1 은 레거시 백엔드(의도된 것) — 변환 대상 아님.

## ⚠️ 이 세션 구조적 장애 (새 세션에서 회복 여부 먼저 확인)
1. **Agent 서브에이전트 채널 사망**: Agent 결과가 `[Tool result missing due to internal error]` 8회 반복, 파일 미변경·리포트 미생성 → 병렬 위임 불가했음. **새 세션은 Agent 1개 먼저 테스트 후 병렬 결정**.
2. **도구출력 간헐 손상**: grep/Read/Bash 가 truncate·라인번호 흔들림·동일줄 반복. 손상 Read 가 가짜 코드 구조를 보여줘 그걸로 Edit 시 "String not found". ★**정확 Read(일관 출력) 확보 후에만 Edit + 매 Edit 후 grep 재검증(동일 명령 2회 일관 출력 확인)**. promotion-log ERROR-202605311630 에 박제.

## 전환 규약 (L2 6파일로 확립 — 그대로 적용)
```python
from core.db import db   # 파일 상단, sys.path 에 PROJECT_DIR(루트) 보장 후 (없으면 import sys; from pathlib import Path; sys.path.insert(0, str(Path(__file__).resolve().parent.parent)))
db.insert(table, row)                  # ← requests.post(f"{url}/rest/v1/{table}", json=row). 삽입 dict 반환(id=data["id"], list면 data[0]["id"])
db.select(table, filters={"col":"op.val"}, order="c.desc", limit=N, select="a,b")
                                       # ← requests.get(...params={col:"op.val","select","order","limit"})
db.update(table, filters, patch)       # ← requests.patch(...?id=eq.X, json=patch)
db.delete(table, filters)              # ← requests.delete
db.rpc_match(table, embedding, column="state_embedding", k=N, filters=None)  # pgvector match_* 대체. row+_similarity 반환
db.execute_raw(sql, params)            # psycopg2 복잡 SQL (SELECT→list[dict], 그외→rowcount). pandas: pd.DataFrame(db.execute_raw(sql, params))
```
- filters = PostgREST 문법 그대로: `eq./ne./gt./gte./lt./lte./in.(a,b)/is.null/like.*x*/ilike.`
- 벡터 컬럼(state_embedding/embedding/decision_embedding)은 **raw list 그대로** 넘김 → 어댑터가 float32 BLOB 직렬화.
- **제거**: `SUPABASE_URL/KEY` 환경변수 가드(`if not url: return`), requests Session/headers 헬퍼, Supabase Management API SQL fallback, 바깥 bash `if [ -n "${SUPABASE_URL:-}" ]` 가드.
- **유지(미변경)**: `skip_trade_db()` 가드, `machine_name`(어댑터가 없는 컬럼 자동 제거), Upbit/외부 API(`api.upbit.com` 등), 임베딩 생성(Gemini/genai/BGE) 로직.
- heredoc/`$PYTHON -c`(.sh): 블록 안 `sys.path.insert(0, '$PROJECT_DIR')` + `from core.db import db` 추가, requests 교체.
- psycopg2: 단순 SELECT/INSERT=db.select/insert, 복잡(JOIN/집계/window)=db.execute_raw.

## SACRED (불변)
- ⛔ `execute_trade.py` 실주문(orders POST·:515·JWT nonce), `rl_hybrid/rl/live_trader.py` run_cycle 본체 미변경. DB I/O만 어댑터화.
- DRY_RUN/EMERGENCY_STOP/MAX_TRADE_AMOUNT 안전장치 기본값 미변경.
- R15/brain M파일 6종(README·core/brain/regime_*·portfolio_orchestrator·progress·progress-inv-v2) 격리. 루트 plan.md·progress.md·progress-inv-v2.md 미변경(격리 신규 파일만).

## 환경
- python = `C:/Users/jsh86/AppData/Local/Programs/Python/Python312/python.exe` (bash 의 `python`/`python3` = command not found — 직전 세션 4h 손실 root cause).
- 셸 = **bash** (cmd 아님). cwd = `/d/projects/Inv`. `cd /d` 금지.
- 검증: `grep -c 'rest/v1' {file}`(0목표) + `"$PY" -c "import ast; ast.parse(open('{file}',encoding='utf-8').read()); print('OK')"`.
- INV_DB_BACKEND=sqlite(기본), data/inv.db.

## 다음 순서
L3(읽기/학습 G1) → G2 → L4(rl_hybrid G3 psycopg2) → G4 → L5(일회성 G5) → G6 통합. 마지막 **L6 회귀**: run_agents.sh DRY_RUN 전체 e2e + `"$PY" -m pytest` 회귀(매매경로 격리 확인).
