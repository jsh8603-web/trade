---
name: consult-kr-rotation-design-RESULTS
description: 한국 12산업 rotation timing 설계 자문 2R 수렴 raw (gemini-web + claude-web 병렬). plan S5.5 / progress 구현 핵심의 SSOT 포인터 대상.
type: reference
tags:
  - domain/eq-kr
  - type/consult-raw
  - tier/study
date: 2026-06-05
consult-rounds: 2 (수렴)
briefing: D:/projects/Inv/.consult-kr-rotation-design-briefing.md
---

# 한국 12산업 rotation timing 설계 자문 — RESULTS (2R 수렴)

> **모델**: Gemini 2.5 Pro (참신/이론) + Claude Opus 4.8 High (실무/정밀). 병렬 2R.
> **briefing**: `.consult-kr-rotation-design-briefing.md` (Q a~e 5문항).
> **본 파일 = raw 박제**. 구현 핵심은 plan S5.5 / progress ckpt 에 반영, 본 파일은 그 SSOT 포인터.

## §0. 수렴 판정

| 판정축 | 결과 |
|---|---|
| (a) 두 모델 의미 일치 | ✅ Q1~Q4 수렴, Gemini가 Claude 정밀화 (1)(2)(3)(4) **전적 동의** |
| (b) 새 의문 비생성 | ✅ 잔여 1건(36셀 폐기 여부)도 estimand 논리로 해소 |
| (c) 3R 내 수렴 | ✅ R2 수렴 |
| (d) trade-off 명확 | ✅ |

**유일 잔여**: Gemini R2 = "3층 36셀이 n=85서 셀당 2.3개월 = 과적합 → 폐기/축소". Claude R2 = "estimand별 effective sample 차이로 36셀 정당(L3 selection 표본 = stock×month 로 큼, L2 rotation 표본 = industry-month 로 작음), nesting 만 하면 됨". → **Claude 우월**(Gemini의 2.3개월 계산은 cross-sectional 종목 차원 무시). ★단 advisory-protocol: measure 단계서 본인 시뮬로 36셀 실제 N<24 collapse 비율 검증 의무.

## §1. 최종 합의 아키텍처 (대안 D Hybrid, Claude-aligned)

**2층 rotation = 정적 residual-PCA sleeve(static 묶음) + 동적 macro-regime tilt(bounded)**. 개별 12산업 rotation = false breadth 로 기각. N_eff ~3-4.

## §2. Q별 수렴 결론 (raw 매핑 — consult-raw-output-mapping-checklist 준수)

### Q-a. 측정 신호 (어느 신호로 robust?)
- **수렴**: 단일 신호 없음. **macro-regime backbone(A) + idiosyncratic-cycle alpha** 가 본체. momentum(B) **강등**(flow dominant → 산업momentum ≈ market momentum×beta = 가짜 breadth, short-horizon reversal에 깨짐 → timing/cost overlay·regime 확인용만). valuation band(C) = slow tilt + **structural-break guard 한정**(조선 슈퍼사이클 실패 = band가 valuation-stationarity 가정하나 re-rating 개시 순간 그 가정 붕괴 = 구조적 wrong-way).
- ★**진짜 alpha = 산업별 idiosyncratic-cycle 지표**: 정유 crack spread / 조선 order backlog·newbuild price / 철강 China PMI·property / 반도체 memory cycle×KRW / bio pipeline×flow-sell duration. 공통 flow factor 와 상관 낮은 부분 = breadth 가 실제 사는 곳. flow-beta 산업(반도체/자동차/금융 cyclical 부분) momentum/valuation = 공통인자 재포장.
- **부호**: short-horizon(1m) reversal 더 신뢰. intermediate(6-12m)는 momentum 아니라 flow persistence 오염 → ★공통인자 residualize 후 부호 재측정 의무.

### Q-b. N_eff 확보 (small cross-section + flow 지배)
- **수렴**: 12개 개별 rotation = 명백한 false breadth. 진짜 독립 차원 ~3-4.
- ★**residualize 먼저, 그다음 sleeve**(Gemini R1 raw-PCA → R2 전적 동의 철회): ① 각 산업 ~ {수출증가율, USDKRW, 외국인순매수, global cyclical} 회귀로 공통 driver 제거 → ② **residual** cross-section 에서 sleeve clustering. raw-return PCA 는 PC1(=Korea risk-on/off)이 분산 60-70% 흡수 → sleeve 무의미.
- **N_eff = residual 상관행렬 participation ratio** = (Σλ_i)²/Σλ_i² (raw 아님).
- ★**비대칭 granularity**: flow-beta redundant 산업은 묶어 noise↓ / idiosyncratic-cycle 산업(정유 crack, 조선 backlog)은 **thin sleeve/singleton+gating 으로 보존**(대형 cyclical sleeve 희석 시 residual alpha 죽음). sleeve membership = §8 static 고정(cluster churn 금지). 동시 active tilt 수 = floor(N_eff) cap, bet sizing = N_eff 에 scale(n=12 아님).

### Q-c. static base + 동적 tilt 결합
- **수렴**: base = **sleeve-level residual risk-parity**(각 sleeve 등위험). rotation = **bounded tilt**.
- ★**tilt 결합 = additive-on-active-share + clip 병행**(R2 핵심 수렴): per-name clip(Δmax)은 단일종목 blowup 만 막고 aggregate active risk 못 잡음 / active-share budget(Σ|Δw|≤τ, τ≈20-40%)은 aggregate 잡으나 단일종목 독식 못 막음 → **clip+budget 병행이 최소 robust**. multiplicative(w_base+clip(c·z))는 ★high-vol·low-base singleton(정유/조선)이 gate 열려도 tilt≈0 = 구조적 함정 → additive 구조적 선호(Gemini도 동의).
- **over-trade 3중 차단**: ① hysteresis no-trade band(|s_new−s_cur| band 넘을 때만 rebalance) ② persistence 요구(regime k개월 지속 or EWMA) ③ cost-aware gate(기대 tilt benefit > turnover cost). 소형시장 sector-timing alpha 대부분 turnover 에 먹힘 → cost gate = 주 필터.
- **κ(공격성) scaling** = program DSR/PSR × N_eff × live-OOS falsification 상태. 약신호 sleeve κ→0 default.

### Q-d. 2층 rotation × 3층 selection 결합 (double-counting 회피)
- **수렴**: ★**자본 = 곱(multiplicative), factor exposure = 더해서 reconcile**. w_j = W_i(산업비중 L2) × v_{j|i}(산업내 종목비중 L3, Σ=1) = fund-of-sleeves 표준.
- ★**double-counting 회피 = L3 within-industry macro-neutrality**: L3 selection 신호를 산업 내 demean → industry 자체 KRW beta 제거 상태로 상대 승자만 pick. macro exposure 전부 L2 소유, L3 = idiosyncratic selection alpha 만. → KRW factor 정확히 1회(L2)만 계산. (Gemini "직교화 잔차" = 동일 본질).
- **검증**: 조립 후 portfolio net common-factor exposure(KRW/flow/export) 산출 → 의도한 L2 tilt 와 대조. 실현 KRW beta > 의도면 초과분 L3서 neutralize. attribution: r_p−r_base ≈ ΣΔW_i·r_i(L2 timing) + ΣW_i·(intra-selection)(L3) + interaction. L3 macro-neutral → interaction 작음 = double-count 없음 증명.

### Q-e. multiplicity + DSR + 약신호 gated
- **수렴**: ★**flat BY(12×K) 버리고 hierarchical sleeve-gatekeeping FDR**. eff_N~4-5 인데 flat 12K 평탄보정 = power 학살. sleeve-level(5×K 고power) 먼저 test → 통과 sleeve 만 within-sleeve(local FDR)로 내려감 = multiplicity 5K+conditional 축소(Yekutieli 2008). **program-level DSR/PSR** = 시도 config 수 trials 반영 단일 deflated SR = "program 켤지" conviction gate(per-sleeve FDR 와 별개). Gemini = 2단계 결합 명시(sleeve DSR 통과 → 생존 sleeve 내 local FDR).
- **MDE/power**: sleeve별 n_OOS MDE + t≈2.802 power 보고. t_obs 임계 바로 아래+저power → "underpowered, gated default 유지, OOS 계속 수집". 점추정 MDE 미만 → 진짜 무alpha → 영구 gate-0. 점추정 박제 금지, HAC/wild-cluster CI 분포.
- ★**continuous gating** κ_sleeve = κ_max × g(evidence), g = {sleeve FDR survival, program DSR, power, live-OOS status} 하향전용 합성. 강신호 sleeve(경제근거+저상관 idiosyncratic+FDR·DSR·power 통과): full κ + e-process/OOS Rank-IC CUSUM live falsification. 약신호(정유/조선/통신 과점): default-0, idiosyncratic 신호(crack/backlog)가 극단+확인+cost통과 시만 small-κ tilt, hysteresis flip 방지.

## §3. R2 정밀화 4쟁점 (Claude 최종, Gemini cross-check 수렴)

| # | 쟁점 | 수렴 결론 |
|---|---|---|
| Q1 | multiplicative vs additive tilt | **clip+budget 병행**(대체 아님, 다른 tail 차원). multiplicative는 low-base singleton서 구조적 불리 → additive 선호 |
| Q2 | 2층 6state vs 3층 36cell | 해상도 차이 = estimand effective sample 차이로 **정당**. 단 독립정의 비일관 → **36셀 atomic partition 고정 + L2 = 36셀→6 surjection 명시 nesting**. L2 통계 = coarse state 내 fine cell 표본가중 pooling(empirical-Bayes 상향집계, 재추정 불필요). aggregation map = ex-ante PIT 고정(prior, fitting 금지). 비대칭 coarsening(KRW/flow 더 뭉개고 export-cycle 보존). audit gate: L2 coarse 부호 = 내부 fine cell pooled 부호 일치 |
| Q3 | singleton N_eff·RP·floor | singleton = N_eff에 ~1 온전 계상(diversifier 보존). base = **sleeve수준 RP 먼저**(각 sleeve 등위험) → singleton sleeve 도 한 sleeve몫 risk budget = 자연 floor. 등위험≠등weight(high-vol singleton low base weight 옳음). ★**tilt 용량 = base weight 비례 금지, standalone IC/Sharpe 신뢰도로 설정**. weight floor 금지(RP 왜곡), sleeve당 최소 risk allocation floor + tilt capacity base 무관 |
| Q4 | DSR 미생존 fallback (0 vs small-κ) | small-κ 중간 fallback **조건부 정당**. 판별축 = ★①adequate-power confident-null(tight CI 0주변) = data 능동반박 → **정직 0** / ②low-power(wide CI 0 straddle, sample-starved) = 반박 아님 → mechanistic PIT-prior 하 **small-κ**. 정당화 4조건 전부: (a) prior+κ를 DSR 보기 전 WeightAssumptionCard PIT 등록 (b) mechanistic/causal prior (c) κ hard-cap data-fit 금지 (d) live falsification 지속(OOS Rank-IC e-CUSUM reject 시 κ→0 decaying). → "rotation 전체 off" 아니라 "low-power∩PIT-mechanistic sleeve 만 decaying small-κ, adequate-power null = 0" = R1 "③실패→전체 gated" 명시 완화 |

## §4. measure 단계 본인 검증 의무 (advisory-protocol §1)
1. ★Gemini "36셀 과적합" claim → 실제 36셀 N<24 collapse 비율 + 셀당 종목×월 표본 실측(Claude estimand 논리 검증).
2. residual N_eff 실측(participation ratio) → raw N_eff 와 비교(residualize 효과 정량).
3. residual cross-section서 어느 산업 생존(idiosyncratic alpha 실재).
4. sleeve-level DSR 생존 여부 → ③ 실패 시 power-conditioned shrinkage fallback.
5. 점추정 박제 금지, 분포+CI(HAC/wild-cluster).

## §5. raw 응답 출처 (task output, 휘발성 — 본 파일이 정리 박제)
- R1 Gemini: response_len 3656 / R1 Claude: 6186
- R2 Gemini: 3340 / R2 Claude: 4880
- 전문 = `~/.claude/.gemini-web-last.md` + `~/.claude/.claude-web-basic-last.md` (diagnostic append)
